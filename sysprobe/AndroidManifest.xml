<?xml version="1.0" encoding="utf-8"?>
<!-- Probe de privilegio: app trivial que so existe para provar que, assinado
     com a test-key publica do AOSP + sharedUserId system, a central o coloca
     em uid 1000. NAO toca no carro, sem servico/receiver, sem permissoes. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.geely.sysprobe"
    android:sharedUserId="android.uid.system"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk android:minSdkVersion="28" android:targetSdkVersion="28" />

    <!-- como uid system essas ja sao concedidas; declarar e inofensivo -->
    <uses-permission android:name="android.car.permission.CAR_POWERTRAIN" />
    <uses-permission android:name="android.car.permission.CAR_ENERGY" />
    <uses-permission android:name="android.car.permission.CAR_INFO" />
    <uses-permission android:name="android.car.permission.CAR_POWER" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <application android:label="SysProbe" android:allowBackup="false">
        <activity android:name=".SysProbeActivity" android:exported="true" android:label="SysProbe">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
