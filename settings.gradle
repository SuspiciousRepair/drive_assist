pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // MPAndroidChart (drivemem's Stats screen) is only published here,
        // not on Maven Central.
        maven { url "https://jitpack.io" }
    }
}

rootProject.name = "drive_assist"
include ":drivemem"
